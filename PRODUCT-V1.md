# Product Algorithm V1

`product-v1.0.0` · 7–10 人局 · 完全离线

给线下《阿瓦隆》的决策支持。一个入口：[`analyzeGame(events, game, options)`](src/lib/decision/analyze.ts)。

---

## 一、它算什么

```
H, I_user  →  P(R | H, I_user)  →  决策评估  →  建议
```

`H` 是公开日志，`I_user` 是用户自己的身份与视野（`role_mark` 私有层）。三层：

**1. 信念 —— 冻结的 Belief Engine V1，精确，不采样**

枚举全部阵营划分（9 人 84 种、10 人 210 种），用规则删掉不可能的，剩下的按行为似然加权；角色层在其上枚举完整发牌（10 人 151,200 种），以 λ=0.4 回火。产出每座位的坏人概率、具体身份概率，以及「在所有还站得住的世界里都成立」的**证明**（只读世界**计数**，绝不读权重——所以没有任何行为假设能造出一个证明）。

**2. 车况 —— 联合量，精确，不采样**

`P(车上坏人数 ≥ 本轮所需坏票数)`，在加权后的世界上求和。这是**联合**概率，不是各座位概率之和——「这两人分开没事、凑一起报警」正是它能表达而线性风险不能表达的东西。同时给出这辆车在**全部合法车**中的风险分位。

**3. 决策 —— 蒙特卡洛 rollout，采样，带误差**

`Q(a) = P(用户这一方获胜 | 采取动作 a)`，把还站得住的世界各推演一遍。两个动作共用同一批世界和同一条随机流，所以差值是**配对**的，噪声大部分抵消。

---

## 二、支持的决策

### 投票

返回上票胜率、下票胜率、ΔQ 及其配对标准误、建议、置信度、解释。

### 点车

在**全部合法车**上穷举精确风险（10 人局 252 辆，毫秒级），取风险最低的若干辆交给 rollout 估胜率，返回推荐、备选、各自崩车概率与胜率估计。

**「穷举」的准确含义**：搜索空间是穷举的，**估值不是**。对 252 辆车每辆跑 400 世界需要数小时，所以精确风险排序决定名次，rollout 只给被推荐的那几辆一个胜率数字。这一点在文档里说清楚，不在代码里含糊。

---

## 三、置信度

不复活旧的「身份熵阈值」。玩家需要知道的是**两个走法差多少**，不是身份解没解出来。

| | 投票 | 点车 |
|---|---|---|
| **strong** | \|ΔQ\| ≥ 2 个标准误 **且** ≥ 3 个百分点 | 比中位合法车干净 ≥ 15 个百分点 |
| **lean** | 过了噪声闸但 < 3 个百分点 | 干净 5–15 个百分点 |
| **too-close** | 落在噪声里 | 全场合法车风险跨度 < 5 个百分点 |

置信度是 `too-close` 时，`recommendation` 返回 **null**——不制造确定性。

### 为什么投票另给一个 `riskDirection`

**这是 V1 最重要的一条已知事实，也是最容易被误读的一条。**

9 人局里你一票只在其余 8 人 4-4 平分时才改变结果。所以**即使一辆必崩的车**，上票和下票的胜率差也只有两三个百分点——在 400 世界的噪声里。实测：梅林看着一辆他知道 100% 会崩的车，ΔQ = −0.018 ± 0.023，判定 `too-close`。

数学没错。但对产品来说「怎么投都行」是坏建议。所以 `riskDirection` 单独存在：**确定性的、不需要 rollout 的、基于车况本身的方向**。它不是关于胜率的主张，所以不叫 recommendation。界面在 ΔQ 分不开时显示它，并说明这是两种不同的判断。

**结论：V1 的投票价值在车况数字，不在 ΔQ。** 40 个真实决策点里，**没有一个**达到 lean 或 strong。这不是缺陷，是这个游戏的结构。

---

## 四、评测结果

### 模拟器校准（纯数学路径，每人数 800 局）

容差在看数字之前写死在 [`release-benchmark.test.ts`](research/release-benchmark.test.ts) 里。**诚实说明：这不是干净的预注册**——同一个模拟器此前的校准结果我已经看过，所以容差是「有信息的」而不是盲的。它们按**什么会改变建议**来定，每一条的理由都写在代码注释里。

| 人数 | 好人胜率 | 任务失败率 | 过车率 | 每轮提案 | 连否5 |
|---|---|---|---|---|---|
| 7 | .400 / .448 ✓ | .451 / .391 ✓ | .632 / .600 ✓ | 1.58 / 1.67 ✓ | .006 / .009 ✓ |
| 8 | **.299 / .414 ✗** | **.534 / .426 ✗** | .554 / .504 ✓ | 1.81 / 1.98 ✓ | .026 / .020 ✓ |
| 9 | .393 / .461 ✓ | .516 / .417 ✓ | .642 / .628 ✓ | 1.56 / 1.59 ✓ | .006 / .005 ✓ |
| 10 | .215 / .311 ✓ | .571 / .477 ✓ | .509 / .466 ✓ | 1.97 / 2.15 ✓ | .049 / .019 ✓ |

（模拟 / 语料）

**8 人局超出两项容差，如实记录、照常发布。** 理由：偏差是**一致方向**的（好人赢得少、任务崩得多），而产品报的是**差值**——一致的水平偏移在 ΔQ 里抵消。10 人局的连否5频率 .049 也贴着 ±.03 的边。

### 推荐稳定性

40 个真实决策点、两个独立种子：ΔQ 同向率 **0.675**。这个数字不高，而它正是「几乎所有投票都被判 too-close」的原因——判据是对的，被判弱的那些**确实**弱。

---

## 五、V1 不依赖什么

- 语言模型 —— 生产路径不发任何网络请求，有测试钉住（拦截 `fetch` 并断言零调用）
- 多智能体社会模拟
- 合成社会信号校准
- 新的梅林/派西维尔/奥伯伦调参
- 新的信念特征

这些全部留在 research 分支，属于 V2。

---

## 六、已知限制

1. **8 人局校准超差**（见上）。偏差方向一致，不改变建议方向。
2. **投票 ΔQ 几乎永远太接近。** 结构性的，不是可修的 bug。产品价值在车况与信念层。
3. **点车是两段式**，不是端到端穷举估值。搜索穷举，估值不穷举。
4. **10 人局分析约 3–5 秒**（枚举 151,200 种发牌 + 400 世界推演）。界面先渲染再算。
5. **模拟器整体偏悲观**：任务失败率系统性高 6–11 个百分点。已知，未调——继续调下去就是在拟合语料统计量而不是在改进模型。
6. **不支持 5–6 人局。** 从未校准过。
7. **假杀不建模。** 三个任务成功后的胜负用语料经验终局率近似。

---

## 七、V1 / V2 边界

| | V1（已发布） | V2（research） |
|---|---|---|
| 信念 | 冻结 Belief V1 | — |
| 社会证据 | 无 | `lib/social` 接口已就位，四通道 |
| LLM | 无（生产路径） | 候选生成、社会证据、闭环推演 |
| 点车 | 穷举风险 + 短名单估值 | LLM 候选 + 数学评估（召回 3.5× 盲选） |

V2 的全部研究结论在 git 历史里，不影响发布。

---

## 八、版本化常量

全在 [`config.ts`](src/lib/decision/config.ts)，每个值都带来源。行为参数（上票率、坏票分布表、提案载荷、λ）属于冻结的 Belief V1，留在 `lib/inference` 里测量它们的代码旁边。

`ALGORITHM_VERSION` 在任何一个值变动时递增。
