# V1 发布检查表

每一项都对应一个可以跑的命令或一个可以点的按钮。没打勾的不发。

## 门槛

- [x] **Belief Engine 保持冻结** — `src/lib/inference/` 本次未改动一行参数
- [x] **投票建议端到端可用** — `analyzeGame` 返回上/下票胜率、ΔQ、置信度、解释
- [x] **点车建议端到端可用** — 全部合法车穷举风险 + 短名单估值
- [x] **重复蒙特卡洛下推荐稳定** — 同种子逐字复现；跨种子同向率已测并记录（0.675）
- [x] **弱局面返回「太接近」** — `recommendation: null`，且不伪造确定性
- [x] **7/8/9/10 人测试全过** — `npx vitest run src/lib/decision/analyze.test.ts`
- [x] **不泄露私有信息** — 视野边界有测试；同一日志换任何隐藏发牌答案不变
- [x] **生产不依赖 LLM** — 测试拦截 `fetch` 并断言零调用
- [x] **模拟器校准缺口已记录** — PRODUCT-V1.md 第四、六节，含 8 人局超差
- [x] **接入现有界面** — 牌桌页「该怎么走」按钮 → `AdvisorSheet`

## 提交前

```bash
npm test                  # 全部单测
npx tsc --noEmit          # 类型
npx eslint src            # 规范
npm run build             # 生产构建
npm run check:imports     # 悬空引用（只 add 自己路径的陷阱）
```

## 发布

1. 在 `dev` 上跑完上面五条
2. 用一局真牌局走完整流程：开局 → 记评价 → 点车 → 投票 → 记任务 → 打开「该怎么走」
3. 确认离线可用（关掉 Wi-Fi 再点一次「该怎么走」，应当照常出结果）
4. 由仓库主人决定合入 `main`

## 不属于本次发布

LLM 候选生成、社会证据通道、闭环社会推演、欺骗消融。全部留在 research，任何一项都不阻塞发布。
