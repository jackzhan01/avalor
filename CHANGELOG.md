# 变更记录

## product-v1.0.0

线下《阿瓦隆》的决策支持，7–10 人局，完全离线。

**新增**

- `analyzeGame(events, game)` —— 一个入口给出信念、车况、建议。界面不需要知道粒子滤波、λ、经验似然表或推演内部的任何东西。
- 牌桌页「该怎么走」：上票还是下票、该点哪辆车。本机计算，不联网，不花钱。
- 建议分三档：**建议** / **略偏向** / **太接近，两边都行**。最后一档返回 null 而不是硬凑一个方向。
- 车况给的是**联合**崩车概率，以及这辆车在全部合法车里的风险分位。

**已知并记录的限制**

- 一个座位的单张票，在 7–10 人局里本来就几乎不改变胜率。所以投票的 ΔQ 绝大多数时候会被诚实地判为「太接近」，产品价值在车况数字。为此单独给了一个不依赖推演的 `riskDirection`。
- 8 人局的模拟器校准超出容差（好人胜率 .299 对语料 .414）。偏差方向一致，在差值里抵消，如实记录后照常发布。
- 10 人局分析约 3–5 秒。

**修复**

- `Action` 里的 `propose` 从来没有被 `playOut` 消费过 —— 每辆候选车的 Q 值完全相同，混合评估无从排序。
- 角色推断的耗时测试原本是 1000ms 的硬阈值，在 CI 上会因为机器负载偶发变红（单跑 367ms，满负载 1198ms，同一份代码）。改成拿七人局做同机基准比较，测的是复杂度而不是机器。

**研究，不影响发布**

社会证据接口、合成信号扫描、LLM 结构臂与社会臂、闭环推演、相关证据折算 —— 全部留在 `research/` 与 `src/lib/social/`，生产路径一行都不碰。
